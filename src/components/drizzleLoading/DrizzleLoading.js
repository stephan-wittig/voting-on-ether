import React from 'react';

// Komponente, die angezeigt werden sollte, wenn Drizzle noch nicht vollständig initialisiert ist.

const DrizzleLoading = ({}) => (
  <div>The Drizzle store is being initialised</div>
);

export default DrizzleLoading;
